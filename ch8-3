#include <stdio.h>
#define PI 3.1415926535897
double cal_area(double radius) {
	double extent = radius * radius * PI;
	return extent;
}
int main(void) {
	double radius;
	scanf_s("%lf", &radius);
	printf("%lf", cal_area(radius));
	
}
