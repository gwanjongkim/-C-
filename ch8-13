#include <stdio.h>
#include <stdlib.h>
#include <time.h>

bool is_multiple(int n, int m) {
	if (n % m == 0) {
		return true;
	}
	else {
		return false;
	}
}
int main(void) {
	int n , m;
	scanf_s("%d %d", & n, &m);
	if (is_multiple(n, m)) {
		printf("it is multiple");
	}
	else {
		printf("it's not multiple");
	}
	

}
