#include <stdio.h>
#include <math.h>

bool is_prime(int n) {
	int cnt = 0;
	for (int i = 2; i < n; i++) {
		if (n % i == 0) {
			cnt +=1;
		}
	}
	return (cnt == 0) ? true : false;
}
int main(void) {
	for (int i = 2; i <= 100; i++ ) {
		if (is_prime(i)) {
			printf("%d ", i);
		}
	}
}
